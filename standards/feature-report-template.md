# 功能开发报告模板

## 功能基本信息

| 字段 | 内容 |
|------|------|
| **功能名称** | |
| **开发日期** | YYYY-MM-DD |
| **完成日期** | YYYY-MM-DD |
| **开发分支** | feature/xxx |
| **关联需求** | |

---

## 功能概述

### 功能描述
[简要描述这个功能是什么]

### 用户价值
[描述这个功能为用户解决了什么问题]

### 涉及模块
- [模块A]
- [模块B]
- [模块C]

---

## 技术方案

### 架构设计

```
[架构图或流程图]

用户界面 → API层 → 业务逻辑 → 数据层 → 存储
```

### 关键文件变更

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/components/...` | 新增 | 组件实现 |
| `src/lib/...` | 修改 | 业务逻辑 |
| `src/app/api/...` | 新增 | API接口 |
| `prisma/schema.prisma` | 修改 | 数据模型 |

### 数据库变更（如有）

```prisma
// 新增模型
model Xxx {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  // ...
}
```

### API 设计（如有）

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/xxx` | POST | 创建xxx |
| `/api/xxx/[id]` | GET | 获取xxx详情 |

---

## 实现细节

### 核心逻辑

```typescript
// 关键代码片段及说明
```

### 边界情况处理

| 场景 | 处理方式 |
|------|----------|
| 网络错误 | 显示错误提示，允许重试 |
| 数据为空 | 显示空状态 |
| 权限不足 | 提示权限不足 |

---

## 测试验证

### 构建验证
- [x] `npm run build` 通过
- [x] TypeScript 类型检查通过
- [x] ESLint 检查通过

### 功能测试

| 测试场景 | 预期结果 | 实际结果 |
|----------|----------|----------|
| 场景A | 结果A | ✅ 通过 |
| 场景B | 结果B | ✅ 通过 |
| 边界测试 | 正确处理 | ✅ 通过 |

### 兼容性
- [ ] 移动端适配正常
- [ ] 旧浏览器兼容（如需要）

---

## 部署清单

- [ ] 数据库迁移已完成
- [ ] 环境变量已配置
- [ ] 新增依赖已安装
- [ ] CDN 资源已更新

---

## 提交记录

```
feat(<scope>): <功能简述>

- 新增功能描述
- 技术实现要点

Closes: #XXX
```

---

## 后续优化

- [ ] 性能优化建议
- [ ] 体验优化建议
- [ ] 自动化测试补充
- [ ] 文档补充
